# Only for faster deploys

version: "3.8"

services:
  app:
    build: .
    container_name: goldic_site
    ports:
      - "127.0.0.1:3001:3001" # expose only to localhost
    expose:
      - "3001"
    restart: unless-stopped
